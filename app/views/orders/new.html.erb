<%= form_for (:order), html: { multipart: true } do |f| %>

 
<div id="search">
  
</div>

<div id="order">

  
</div>

<div id="latest_order">
  
</div>

<div id="list_products">
<table>
<tr>
  <% @product.each do |product| %>
  <span>
  <table style="display: inline">
  <tr>
  <td><%= image_tag product.image.url(:thumb), :class => "allProducts", :id => product.id %></td>
  </tr>
  <tr>
  <td style="text-align: center"><%= product.name %></td>
  </tr>
  </table>
  </span>
  <% end %>


</div>

<% end %>